export interface IConstants {
  baseColor: string;
  center: number;
  canvasContext: "2d" | "webgl" | "webgl2" | "bitmaprenderer";
  fenceRadius: number;
  fontColor: string;
  lineWidth: number;
  middleCircleColor: string;
  middleCircleRadius: number;
  outerCircleColor: string;
  outerCircleRadius: number;
  sideLength: 600;
}
